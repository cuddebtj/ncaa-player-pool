#!/usr/bin/env bash
# Wrapper script to run NCAA Player Pool CLI
# This ensures environment variables are loaded fresh from .env

cd "$(dirname "$0")"

# Unset any cached environment variables to force reload from .env
unset POSTGRES_CONN_STR

exec uv run ncaa-pool "$@"
